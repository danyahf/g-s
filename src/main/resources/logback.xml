<configuration>

    <conversionRule conversionWord="txId" converterClass="com.danya.logging.TransactionIdConverter"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%txId%n
            </pattern>
        </encoder>
    </appender>

    <appender name="ACCESS_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level ACCESS - %msg%txId%n
            </pattern>
        </encoder>
    </appender>

    <logger name="ACCESS" level="INFO" additivity="false">
        <appender-ref ref="ACCESS_CONSOLE"/>
    </logger>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
